#server:
#  port: 8761  # Eureka typically runs on port 8761
#
#spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/microservices
#    username: root
#    password: root
#    driver-class-name: com.mysql.cj.jdbc.Driver
#  application:
#    name: USER-SERVICE
#  jpa:
#    hibernate:
#      ddl-auto: update
#
#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: false  # Changed from true to false
#    register-with-eureka: false  # Changed from true to false
#    service-url:
#      defaultZone: http://localhost:8761/eureka  # Changed port to 8761


#Configuration with hotel service
server:
  port: 8081  # Different port from Eureka server

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservices
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: USER-SERVICE
  jpa:
    hibernate:
      ddl-auto: update
  config:
    import: configserver:http://localhost:8085

#    Configure dev, default or prod server by just adding this
#  profiles:
#    active:
#      - prod


##Configure with eureka registry service
#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true  # Should be true for clients
#    register-with-eureka: true  # Should be true for clients
#    service-url:
#      defaultZone: http://localhost:8761/eureka  # Points to Eureka server

management:
  health:
    circuitbreakers:
      enabled: true
  endpoints:
    web:
      exposure:
        include:
          - health
  endpoint:
    health:
      show-details: ALWAYS

resilience4j:
  circuitbreaker:
    instances:
      ratingHotelBreaker:
        registerHealthIndicator: true
        event-consumer-buffer-size: 10
        failure-rate-threshold: 50
        minimum-number-of-calls: 5
        automatic-transition-from-open-to-half-open-enabled: true
        wait-duration-in-open-state: 6s
        permitted-number-of-calls-in-half-open-state: 3
        sliding-window-size: 10
        sliding-window-type: COUNT_BASED


